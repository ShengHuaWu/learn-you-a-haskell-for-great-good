-- Import the entire module (inside only one file)
import Geometry

-- Import sub-modules (defined in different files but inside the same folder)
import qualified Geometry.Sphere as Sphere  
import qualified Geometry.Cuboid as Cuboid  
import qualified Geometry.Cube as Cube 